
const pen = { brand: 'echono', price: 5, color: 'black'}

/* --another way to object declare--- */
const pencil =  new Object();//emty object but amra chaile value diye dite pari
// console.log(pencil);

/* ---another way to declare object--- */
const rubber = Object.create({});//emty object but amra chaile value diye dite pari
console.log(rubber);